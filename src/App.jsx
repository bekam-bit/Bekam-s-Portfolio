import Home from './pages/Home.jsx'; // Import the Home component created earlier

// App.jsx serves as the main application container
export default function App() {
  return (
    // You can optionally add a general layout wrapper here, 
    // but in this setup, the 'Home' component handles the main layout.
    <Home />
  );
}